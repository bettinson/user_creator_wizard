<%= render "header" %>

<%= form_tag next_wizard_path, method: :put do %>
  <%= select_tag :color, options_for_select(UserValidations.valid_colors)%>
  <%= text_field_tag :other, nil, placeholder: 'Other color' %>
  <%= submit_tag 'Finish' %>
<% end %>
